# Secrets — never publish
auth.json

# Runtime state (regenerable / machine-specific)
sessions/
models-store.json
trust.json
settings.json
models.json

# macOS
.DS_Store

# Backups created by setup.sh
*.bak

# Local npm packages installed by pi
npm/

# pi-managed package clones (regenerable via pi install)
git/

# Runtime state incl. pi-undo-redo workspace snapshots (may contain file contents)
state/

# Extension dependencies are installed from their tracked package-lock.json files
node_modules/
